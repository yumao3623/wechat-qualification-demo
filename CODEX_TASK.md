# CODEX_TASK.md

# 企业政府资质预评估微信小程序 Demo

## 1. 项目目标

设计并实现一个可以完整运行的微信小程序 Demo。

产品目标：

结合企业公开信息以及用户补充的经营数据，在约 1 分钟内对企业是否可能符合以下四类政府相关企业资质进行预评估：

1. 高新技术企业
2. 科技型中小企业
3. 专精特新
4. 雏鹰企业

产品核心能力：

* 搜索企业；
* 确认企业主体；
* 构建企业画像；
* 识别影响评估的缺失数据；
* 动态要求用户补充缺失信息；
* 根据规则进行预评估；
* 输出可解释诊断报告；
* 展示判断证据；
* 展示差距和行动建议；
* 引导用户联系顾问；
* 为后续一对一申报服务建立转化入口。

本产品只是：

“资质预评估工具”。

不是政府官方认定系统。

不得保证企业一定符合资质或一定可以获得补贴。

---

# 2. 产品原则

## 2.1 低门槛体验

用户进入小程序后：

不要求登录。

不自动授权。

不弹手机号授权。

用户可以先体验产品主要前置流程。

---

## 2.2 先利用已有数据

优先使用企业公开信息构建企业画像。

不要一开始就要求用户填写大量数据。

---

## 2.3 动态补数据

只有当评估规则需要某项数据、并且企业公开数据中缺失时：

才要求用户补充。

避免一个巨大的固定表单。

---

## 2.4 可解释评估

评估结果不能只有：

“符合 / 不符合”。

报告应该解释：

* 为什么这样判断；
* 使用了什么数据；
* 哪些条件已满足；
* 哪些条件不满足；
* 哪些条件无法判断；
* 用户下一步可以做什么。

---

# 3. Tab 结构

底部共有三个 Tab。

---

## Tab 1：首页

定位：

* 营销钩子；
* 产品价值说明；
* 开始诊断；
* 四类资质介绍。

登录要求：

免登录。

---

## Tab 2：报告

定位：

用户的诊断报告列表。

可能状态：

* 未登录；
* 无报告；
* 诊断进行中；
* 已完成；
* 失败。

登录要求：

查看保存的报告数据需要登录。

未登录时：

显示合理引导。

禁止进入 Tab 后自动触发授权。

---

## Tab 3：我的

定位：

帮助、法律和账户相关功能。

登录要求：

免登录。

至少包含：

* 使用说明；
* 隐私政策；
* 用户协议；
* 免责声明；
* 退出登录。

如果当前未登录：

可以不显示退出登录。

---

# 4. 页面清单

必须覆盖以下页面和交互。

## 首页

类型：

Tab 页面。

登录：

免登录。

主要内容：

* 产品价值介绍；
* “1 分钟预评估”概念；
* 四类资质入口或介绍；
* 开始诊断 CTA；
* 产品免责声明提示。

---

## 企业搜索 Step 1

类型：

普通页面。

登录：

免登录。

功能：

用户输入企业名称关键词。

系统返回匹配企业。

Demo 模式使用：

虚构 Mock 企业数据。

不要假装虚构企业是真实企查查数据。

---

## 主体确认 Step 1

类型：

普通页面。

登录：

免登录。

目的：

避免用户选择错误企业。

展示：

* 企业名称；
* 统一社会信用代码或 Demo 标识；
* 法定代表人；
* 成立日期；
* 注册资本；
* 所在地区；
* 企业状态；
* 行业。

允许用户：

确认主体。

也允许：

返回重新搜索。

---

## 企业画像 Step 2

类型：

普通页面。

登录：

免登录。

展示系统当前掌握的企业信息。

至少区分：

* 已获取信息；
* 缺失信息；
* 需要用户补充的信息。

可展示：

* 基本工商信息；
* 企业年龄；
* 行业；
* 地区；
* 员工数量；
* 研发人员；
* 营业收入；
* 研发投入；
* 知识产权；
* 科技相关资质；
* 其他影响判断的指标。

不一定所有字段都有数据。

---

## 补充经营数据 Step 3

类型：

普通页面。

登录：

免登录。

这个页面是动态页面。

只展示：

当前企业评估所需要、但现有数据缺失的字段。

可能字段包括：

* 年营业收入；
* 研发费用；
* 研发人员人数；
* 企业总人数；
* 知识产权；
* 主营业务收入；
* 特定领域收入；
* 研发成果；
* 认证或资质；
* 其他业务指标。

实际字段应该根据：

规则引擎需求 + 当前企业缺失数据

动态决定。

---

# 5. 发起诊断弹窗

用户点击：

“发起诊断”

后显示。

此时必须：

展示必要协议提示。

至少涉及：

* 用户协议；
* 隐私政策；
* 免责声明。

协议 checkbox：

默认不得勾选。

即：

初始状态必须为 `false`。

如果用户没有同意：

不能发起诊断。

如果用户关闭弹窗：

不能强制登录。

用户仍然可以继续以游客身份浏览。

只有用户明确同意协议之后：

才建立登录 Session 并发起诊断。

---

# 6. 登录设计

进入小程序时：

禁止自动登录。

禁止自动弹授权。

禁止强制手机号授权。

以下页面游客可以访问：

* 首页；
* 企业搜索；
* 主体确认；
* 企业画像；
* 补充经营数据；
* 联系顾问；
* 使用说明；
* 隐私政策；
* 用户协议；
* 免责声明。

登录只在以下情况需要：

1. 用户确认发起诊断；
2. 用户需要查看个人报告数据。

使用：

`wx.login`

建立 Demo Session。

必须在文档中说明：

Demo 登录与生产环境登录的区别。

---

# 7. 诊断进度 Step 4

类型：

普通页面。

登录：

已登录。

用于展示诊断过程。

可以设计为以下阶段：

1. 准备企业数据；
2. 检查数据完整性；
3. 执行高企规则；
4. 执行科技型中小企业规则；
5. 执行专精特新规则；
6. 执行雏鹰规则；
7. 整理证据；
8. 生成报告。

Demo 可以模拟异步诊断。

但是必须有真实状态流转：

例如：

`pending`

→ `processing`

→ `ready`

或：

→ `failed`

不能只用一个固定页面假装加载。

---

# 8. 诊断报告 Step 4

类型：

普通页面。

登录：

已登录。

必须同时展示四类资质预评估：

* 高新技术企业；
* 科技型中小企业；
* 专精特新；
* 雏鹰企业。

建议状态：

* 较有希望；
* 存在机会；
* 需补充数据；
* 暂不满足；
* 不适用。

禁止使用：

* 保证通过；
* 一定符合；
* 可以直接获得补贴；
* 官方认定成功。

报告必须明显说明：

本结果仅供初步判断和准备工作参考，最终结果以相关主管部门政策、申报通知及审核结果为准。

---

# 9. 证据链明细

类型：

普通页面。

登录：

已登录。

目的：

解释报告为什么得出当前结果。

每项判断可以包括：

* 条件名称；
* 政策要求；
* 企业实际数据；
* 数据来源；
* 判断；
* 是否缺数据；
* 说明。

示例：

条件：

研发人员比例。

要求：

≥ 某比例。

企业数据：

研发人员 20 人 / 员工 150 人。

来源：

用户补充。

判断：

满足。

这里的实际规则必须由 Rule Engine 返回。

不要在页面中重新实现业务判断。

---

# 10. 缺口与行动清单

类型：

普通页面。

登录：

已登录。

针对：

* 不满足条件；
* 缺失数据；
* 暂时无法判断。

生成行动清单。

每个 Action 可以包含：

* 缺口；
* 为什么重要；
* 建议行动；
* 优先级；
* 是否建议咨询顾问。

例如：

缺口：

知识产权数量不足。

行动：

检查现有软件著作权、专利及正在申请的知识产权情况，并确认是否满足相关认定要求。

不要把建议写成官方保证。

---

# 11. 联系顾问

类型：

普通页或弹窗。

登录：

不强制。

作用：

用户希望进一步获得一对一申报咨询时提交线索。

可以允许：

* 姓名；
* 手机号；
* 企业名称；
* 咨询方向；
* 备注。

手机号授权可以作为可选能力。

不要为了 Demo 强制要求微信手机号授权。

提交成功后：

显示：

“咨询需求已提交”。

不要写：

“顾问已接单”

除非系统真的实现了该流程。

---

# 12. 企业数据设计

禁止：

直接爬取企查查网页。

必须设计：

Enterprise Data Provider。

建议接口：

`searchEnterprises(keyword)`

`getEnterpriseById(id)`

实现：

`MockEnterpriseProvider`

未来预留：

`QichachaEnterpriseProvider`

Demo 必须在：

无企查查 API Key

情况下完整运行。

---

# 13. Mock 企业设计

至少准备 4 组虚构企业。

## Scenario A

数据相对完整。

多个资质预评估结果：

较有希望。

---

## Scenario B

缺少研发、收入或知识产权信息。

部分结果：

需补充数据。

---

## Scenario C

企业明显不满足若干关键条件。

结果：

暂不满足。

---

## Scenario D

企业所在地不符合某个区域性政策。

对应资质：

不适用。

所有 Mock 数据必须明确是：

Demo 虚构数据。

---

# 14. 规则引擎

建立独立的：

`QualificationEngine`

建议包含：

`HighTechEnterpriseEvaluator`

`TechSMEEvaluator`

`SpecializedInnovativeEvaluator`

`EagleEnterpriseEvaluator`

每个 Evaluator 返回结构化结果。

建议格式：

```js
{
  qualificationType,
  status,
  summary,
  criteria,
  evidence,
  missingFields,
  gaps,
  actions,
  ruleVersion
}
```

规则引擎不能直接依赖：

页面代码。

应该可以单独测试。

---

# 15. 动态表单逻辑

动态表单不能只是假名字。

需要真正根据：

规则引擎所需字段

与：

企业当前已有字段

计算缺失项。

例如：

规则需要：

`annualRevenue`

`rdExpense`

`employeeCount`

`rdEmployeeCount`

企业已有：

`annualRevenue`

缺失：

`rdExpense`

`employeeCount`

`rdEmployeeCount`

那么 Step 3 只显示后三项。

补充完成后：

把数据加入诊断输入。

---

# 16. 后端能力

实现轻量后端。

至少包含以下业务能力：

## 企业

* 搜索企业；
* 查询企业详情。

## 登录

* 接收微信登录 Code 的 Demo 流程；
* 建立 Session；
* 查询当前 Session。

## 诊断

* 创建诊断；
* 查询诊断状态；
* 获取诊断报告。

## 报告

* 当前用户报告列表；
* 单个报告详情。

## 顾问

* 提交咨询 Lead。

具体 Endpoint 名称可以自行设计。

但是必须在：

`docs/architecture.md`

记录。

---

# 18. 状态设计

主要页面应该考虑：

## Loading

例如：

企业搜索中。

诊断处理中。

报告加载中。

---

## Empty

例如：

没有搜索结果。

暂无报告。

---

## Error

例如：

API 请求失败。

诊断失败。

---

## Success

例如：

诊断完成。

顾问咨询提交成功。

---

# 19. PRD 文档要求

生成：

`docs/PRD.md`

至少包含：

* 项目背景；
* 目标用户；
* 用户痛点；
* 产品目标；
* 非目标；
* 用户场景；
* 用户旅程；
* 信息架构；
* 页面结构；
* 功能需求；
* 登录设计；
* 隐私设计；
* 动态表单逻辑；
* 规则引擎需求；
* 报告结构；
* 顾问转化；
* 异常场景；
* 验收标准。

---

# 20. design.md 要求

生成：

`docs/design.md`

至少包含：

* 设计目标；
* UI 风格；
* 页面层级；
* 首页布局；
* 搜索页布局；
* 主体确认页；
* 企业画像页；
* 动态表单；
* 协议弹窗；
* 诊断进度；
* 诊断报告；
* 证据链；
* 缺口行动页；
* 报告 Tab；
* 我的 Tab；
* 联系顾问；
* Loading；
* Empty；
* Error；
* Success；
* 核心交互规则。

---

# 21. architecture.md 要求

生成：

`docs/architecture.md`

至少包含：

* 系统架构图；
* 项目目录；
* 小程序前端结构；
* 后端结构；
* API 设计；
* 数据模型；
* Login Session；
* Enterprise Provider；
* Qualification Engine；
* Dynamic Form；
* Report Generator；
* 数据持久化；
* Mock 模式；
* Production 模式；
* 安全设计；
* 密钥管理；
* 后续真实企查查接入方式。

---

# 22. test-cases.md 要求

生成：

`docs/test-cases.md`

每个测试用例至少包含：

| 字段           | 内容                         |
| ------------ | -------------------------- |
| Test ID      | 唯一编号                       |
| Module       | 模块                         |
| Precondition | 前置条件                       |
| Steps        | 操作步骤                       |
| Expected     | 预期结果                       |
| Status       | PASS / FAIL / NOT EXECUTED |

必须覆盖：

* 首页游客访问；
* 企业搜索；
* 无结果；
* 企业确认；
* 企业画像；
* 动态字段；
* 补数据；
* 协议默认未勾选；
* 不同意协议；
* 同意协议；
* 登录；
* 诊断；
* 诊断失败；
* 报告；
* 证据链；
* 缺口；
* 报告 Tab；
* 未登录报告 Tab；
* 顾问咨询；
* 后端 API；
* Rule Engine。

---

# 23. README 要求

根目录必须生成：

`README.md`

让评审人员能够根据 README 运行项目。

至少包含：

* 项目名称；
* 项目简介；
* 产品目标；
* 项目结构；
* 技术栈；
* 环境要求；
* 安装依赖；
* 后端启动；
* 微信开发者工具导入；
* 本地 API 配置；
* Mock 企业列表；
* 推荐演示企业；
* 完整 Demo 演示流程；
* 测试命令；
* 环境变量；
* 已知限制；
* 真实上线需要完成的工作。

---

# 24. 完整 Demo 验收场景

至少必须跑通下面这一条完整流程。

用户首次进入小程序。

→ 不登录。

→ 不出现授权弹窗。

→ 浏览首页。

→ 点击开始诊断。

→ 搜索一家虚构企业。

→ 选择企业。

→ 查看主体信息。

→ 确认企业。

→ 查看企业画像。

→ 系统发现部分数据缺失。

→ 用户补充必要经营数据。

→ 点击发起诊断。

→ 出现协议弹窗。

→ 协议 checkbox 默认没有勾选。

→ 用户同意协议。

→ 系统建立 Session。

→ 创建诊断任务。

→ 展示诊断进度。

→ 诊断完成。

→ 显示四类资质预评估。

→ 打开其中一个资质。

→ 查看判断证据。

→ 查看差距和行动建议。

→ 返回报告。

→ 报告出现在“报告”Tab。

→ 用户可以提交顾问咨询。

该流程必须能够由招聘方在本地复现。

---

# 25. 开发优先级

严格按照以下优先级：

## Priority 1

完整业务流程可以跑通。

## Priority 2

登录、协议、隐私逻辑正确。

## Priority 3

产品页面结构合理。

## Priority 4

Rule Engine 可以解释。

## Priority 5

PRD / Design / Architecture / Tests 完整。

## Priority 6

视觉和动画优化。

不得为了：

动画、炫技、复杂架构

牺牲核心流程。

---

# 26. Demo 明确不要求

本 Demo 不需要：

* 正式支付；
* 正式 CRM；
* 正式顾问调度；
* 正式短信系统；
* 大规模云架构；
* 完整生产账号体系；
* 政府系统直连；
* 官方审批；
* 正式资格认定；
* 保证补贴结果；
* 完整企业数据库；
* 完整生产安全体系。

如果生产上线需要：

在文档说明即可。

不要假装 Demo 已经具备。
