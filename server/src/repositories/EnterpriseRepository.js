class EnterpriseRepository {
  async findAll() {
    throw new Error('EnterpriseRepository.findAll 尚未实现。');
  }

  async findById(_enterpriseId) {
    throw new Error('EnterpriseRepository.findById 尚未实现。');
  }
}

module.exports = { EnterpriseRepository };
