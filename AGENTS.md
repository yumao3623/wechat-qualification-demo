# AGENTS.md

## 1. 项目角色

你是本仓库的主要软件工程师和产品实现 Agent。

本项目是一个用于招聘能力验证的微信小程序 Demo，目标是对企业是否可能符合政府相关资质/补贴条件进行约 1 分钟的预评估。

覆盖四类资质：

1. 高新技术企业
2. 科技型中小企业
3. 专精特新
4. 雏鹰企业

你的职责不仅是生成代码，还必须确保：

* 正确理解需求；
* 架构简单、清晰、可解释；
* 完整 Demo 能够实际运行；
* 产品文档和实际代码保持一致；
* 重要技术和产品决策有记录；
* 测试覆盖关键用户流程；
* 不得为了方便开发而擅自删除或弱化重要需求；
* 登录、隐私、协议等合规要求必须严格落实。

本项目是招聘 Demo，不是大型生产系统。

始终优先：

1. 可运行；
2. 可演示；
3. 可解释；
4. 结构清晰；
5. 易于维护和检查。

不要为了体现技术能力而过度设计。

---

# 2. 强制工作流程

在实现任何重要功能之前：

1. 阅读根目录 `CODEX_TASK.md`；
2. 阅读相关的 `docs/` 文档；
3. 检查当前已有代码；
4. 明确本次预计修改哪些文件；
5. 实现最小但完整的一组功能；
6. 执行相关测试或验证；
7. 修复发现的问题；
8. 如果实现与文档不一致，必须同步更新文档。

禁止只因为“文件已经生成”就认为任务完成。

只有主要业务路径已经实际运行或经过合理验证，才能认为该功能完成。

---

# 3. 范围控制

不要添加与招聘题目无关的功能。

禁止为了“架构先进”而主动引入：

* 微服务；
* Kubernetes；
* 分布式系统；
* 消息队列；
* 大型 DevOps 系统；
* 复杂权限系统；
* 不必要的设计模式；
* 与 Demo 无关的技术框架。

这是招聘 Demo，不是正式政府申报平台。

优化目标是：

* 产品逻辑清晰；
* 架构容易理解；
* 完整用户流程可以跑通；
* 本地启动可靠；
* 代码结构清晰；
* 方便演示；
* 面试时能够解释设计原因。

如果需求存在歧义：

1. 优先采用最适合 Demo 的合理方案；
2. 不要偷偷自行决定；
3. 把重要假设记录在：

`docs/decisions.md`

---

# 4. 推荐技术方向

优先使用简单、稳定、容易运行的技术方案。

## 微信小程序前端

优先使用：

* 原生微信小程序；
* WXML；
* WXSS；
* JavaScript；
* 微信官方 Mini Program API。

除非有充分理由，否则不要主动引入复杂跨端框架。

---

## 后端

优先使用：

* Node.js；
* Express 或其他轻量 HTTP Framework；
* REST API；
* 尽可能少的第三方依赖。

Demo 阶段允许使用：

* JSON；
* 本地文件；
* SQLite；
* 其他轻量级持久化方式。

前提是：

代码结构必须允许未来替换为正式数据库。

---

# 5. 企业数据要求

禁止直接爬取企查查网页。

企业数据必须通过 Provider 抽象层实现。

至少实现：

`MockEnterpriseProvider`

并预留未来：

`QichachaEnterpriseProvider`

Demo 必须满足：

即使没有真实企查查 API Key，也可以完整运行。

Mock 数据必须：

* 使用虚构企业；
* 明确标记为 Demo 数据；
* 不得冒充真实企查查数据；
* 能覆盖多种评估结果。

禁止把任何真实 API 密钥写入代码。

如果未来实现真实企查查 API：

所有凭证必须来自环境变量，例如：

`.env`

同时提供：

`.env.example`

真实 API Key 不得提交到 Git 仓库。

---

# 6. 资质评估引擎

本系统只提供：

“预评估”

而不是官方认定。

系统禁止使用以下表达：

* 保证通过；
* 一定符合；
* 已获得资质；
* 官方认定通过；
* 保证获得补贴。

推荐状态包括：

* 较有希望；
* 存在机会；
* 需补充数据；
* 暂不满足；
* 不适用。

每一个评估结果都应该具有可解释性。

在合理情况下，每项规则输出应包含：

* `criterion`：判断条件；
* `requirement`：要求；
* `actualValue`：企业当前数据；
* `source`：数据来源；
* `result`：判断结果；
* `missingData`：缺失信息；
* `action`：建议行动。

---

# 7. 规则层必须与 UI 分离

禁止将资质判断逻辑直接写入：

* WXML 页面；
* 小程序页面事件函数；
* HTTP Route Handler。

评估规则必须属于独立业务层。

推荐概念结构：

`QualificationEngine`

下面分别包含：

`HighTechEnterpriseEvaluator`

`TechSMEEvaluator`

`SpecializedInnovativeEvaluator`

`EagleEnterpriseEvaluator`

规则引擎应该能够脱离 UI 独立测试。

---

# 8. 四类资质

Demo 需要覆盖：

1. 高新技术企业；
2. 科技型中小企业；
3. 专精特新；
4. 雏鹰企业。

政府政策可能发生变化。

不要把 Demo 中的简化规则描述成：

“官方完整规则”。

必须在文档中记录：

* 政策依据；
* 规则版本或日期；
* Demo 中采用了哪些简化；
* 哪些条件无法自动判断；
* 哪些条件需要人工核验；
* 哪些政策存在地域限制。

如果“雏鹰企业”等资质存在地区差异，必须明确体现适用地区。

---

# 9. 登录和隐私要求

这是强制要求，不允许擅自修改。

用户进入微信小程序时：

禁止：

* 自动登录；
* 自动调用手机号授权；
* 自动弹授权框；
* 强制用户同意协议。

游客必须能够访问：

* 首页；
* 企业搜索；
* 主体确认；
* 企业画像；
* 补充经营数据；
* 使用说明；
* 隐私政策；
* 用户协议；
* 免责声明。

只有以下行为才需要建立登录会话：

1. 发起诊断；
2. 查看保存的报告。

---

# 10. 发起诊断流程

用户点击：

“发起诊断”

之后：

先显示相关协议和隐私说明。

协议复选框：

默认必须为：

`false`

即：

默认不勾选。

如果用户：

* 不勾选；
* 拒绝；
* 关闭弹窗；

系统必须：

* 不开始诊断；
* 不强制登录；
* 不阻止继续浏览；
* 用户仍然可以使用游客功能。

只有用户明确同意后，才进入登录和诊断流程。

在合适的阶段使用：

`wx.login`

Demo 登录逻辑与正式生产认证必须在文档中区分。

不要假装 Demo Session 已经具备生产安全等级。

---

# 11. 必须跑通的完整用户流程

以下主流程必须保持可运行：

首页

→ 企业搜索

→ 主体确认

→ 企业画像

→ 必要时补充经营数据

→ 点击发起诊断

→ 阅读并同意协议

→ 建立登录 Session

→ 诊断进度

→ 诊断报告

→ 证据链明细

→ 缺口与行动清单

→ 联系顾问

---

# 12. 报告 Tab 要求

报告 Tab 至少支持以下状态：

* 未登录；
* 无报告；
* 诊断进行中；
* 已完成；
* 失败。

如果未登录：

不要自动弹授权。

应该显示：

* 为什么需要登录；
* 登录后可以查看什么；
* 引导用户继续完成诊断。

---

# 13. 我的 Tab

必须包含：

* 使用说明；
* 隐私政策；
* 用户协议；
* 免责声明；
* 退出登录。

游客可以访问“我的”基本页面。

只有已经登录时才显示：

退出登录。

---

# 14. Demo 质量要求

禁止只做一组没有逻辑的静态页面。

重要交互必须真正有状态变化。

例如：

企业搜索按钮必须真正返回 Demo 企业。

主体确认必须真正进入下一步。

补充数据必须能够影响诊断。

发起诊断必须真正创建诊断任务。

诊断进度必须最终产生报告。

报告必须可以进入证据链和行动清单。

顾问按钮必须能够完成 Demo 留资。

重要按钮不能只是：

`console.log()`

页面必须考虑：

* Loading；
* Empty；
* Error；
* Success。

最终版本不要留下明显未完成文本，例如：

`TODO`

`Lorem ipsum`

`Coming soon`

除非该功能明确属于非 Demo 范围。

---

# 15. Mock 企业数据

必须提供多种虚构企业，用于演示不同结果。

建议至少包括：

## 企业 A

数据相对完整。

多个资质结果较好。

用于展示：

“较有希望”。

## 企业 B

缺失研发、人员或知识产权等信息。

用于展示：

“需补充数据”。

## 企业 C

多个关键指标不满足。

用于展示：

“暂不满足”。

## 企业 D

地区不匹配。

用于展示地区限定资质：

“不适用”。

Mock 企业必须明确标注为虚构 Demo 数据。

---

# 16. 文档要求

必须维护以下文件：

## `docs/PRD.md`

包含：

* 项目背景；
* 产品目标；
* 用户角色；
* 用户需求；
* 用户流程；
* 信息架构；
* 页面需求；
* 功能需求；
* 登录和隐私逻辑；
* 业务规则；
* 异常情况；
* 验收标准；
* Demo 范围；
* 非目标范围。

---

## `docs/design.md`

包含：

* UI 设计方向；
* 页面结构；
* 页面布局；
* 页面交互；
* Loading 状态；
* Empty 状态；
* Error 状态；
* Success 状态；
* 重要组件；
* 报告展示方式；
* 表单交互；
* 用户操作反馈。

---

## `docs/architecture.md`

包含：

* 系统整体架构；
* 项目目录；
* 前端架构；
* 后端架构；
* API；
* 登录流程；
* Enterprise Provider；
* Assessment Rule Engine；
* 数据存储；
* 安全考虑；
* Mock 与生产模式差异；
* 未来升级真实系统时需要做什么。

---

## `docs/test-cases.md`

测试用例至少包含：

* Test ID；
* 模块；
* 前置条件；
* 操作步骤；
* 预期结果；
* 执行状态。

必须覆盖：

* 游客访问；
* 企业搜索；
* 企业确认；
* 企业画像；
* 动态补充表单；
* 协议；
* 登录；
* 发起诊断；
* 诊断状态；
* 报告；
* 证据链；
* 缺口；
* 顾问；
* API；
* Rule Engine；
* 异常情况。

---

## `docs/decisions.md`

用于记录：

* 产品假设；
* 技术选型；
* 简化方案；
* 业务规则假设；
* 地区限制；
* Mock 处理；
* 无法确定的需求；
* 为什么做出某个设计选择。

重要决策不得只存在于聊天回复中。

---

# 17. 测试要求

至少测试：

* 企业搜索；
* 企业选择；
* 企业详情；
* 缺失数据识别；
* 动态表单字段生成；
* 四类资质规则；
* 诊断创建；
* 报告生成；
* 报告查询；
* 未登录访问报告；
* Session；
* 协议默认未勾选；
* 顾问留资。

后端和规则引擎发生修改之后：

必须重新执行相关测试。

如果微信小程序 UI 无法完全自动测试：

必须提供人工 E2E Test Checklist。

禁止把：

“未执行”

写成：

“测试通过”。

测试状态统一使用：

`PASS`

`FAIL`

`NOT EXECUTED`

---

# 18. 安全和敏感信息

禁止提交：

* API Key；
* Qichacha Secret；
* 微信 AppSecret；
* Token；
* 生产数据库密码。

所有敏感信息使用环境变量。

提供：

`.env.example`

不要把：

微信 AppSecret

放在微信小程序前端代码中。

后端 API 应进行基本输入校验。

前端不应该直接显示内部错误堆栈。

---

# 19. README 要求

根目录必须提供：

`README.md`

必须包含：

* 项目简介；
* 产品目标；
* 技术架构；
* 环境要求；
* 安装方式；
* 后端启动方法；
* 微信开发者工具导入方法；
* 环境变量配置；
* Demo 企业；
* 完整演示流程；
* 测试方法；
* 已知限制；
* 生产环境还需要完成哪些工作。

一个第一次看到这个项目的人，应该可以根据 README 独立启动 Demo。

---

# 20. 完成定义 Definition of Done

项目只有同时满足以下条件，才可以认为完成：

* 微信小程序能够打开；
* 核心用户流程可以跑通；
* 后端可以正常启动；
* Mock 企业搜索可用；
* 用户可以查看企业画像；
* 缺失数据可以补充；
* 协议默认不勾选；
* 登录触发时机符合要求；
* 可以发起诊断；
* 可以生成报告；
* 四类资质都有结果；
* 报告可以解释判断依据；
* 可以查看证据链；
* 可以查看缺口行动清单；
* 可以提交顾问咨询；
* 报告 Tab 可以正常工作；
* 文档齐全；
* 文档与代码一致；
* 测试结果有记录；
* README 能指导运行；
* 仓库不存在明文敏感凭证；
* 已知限制明确写出。

---

# 21. 每个阶段结束后的输出格式

每完成一个重要阶段，都必须总结：

## 已完成

列出本阶段完成的功能。

## 修改文件

列出主要修改的文件。

## 实际执行

列出实际运行过的命令和测试。

## 测试结果

使用：

PASS / FAIL / NOT EXECUTED

## 当前问题

列出仍然存在的问题。

## 下一步建议

说明下一阶段应该做什么。

禁止只回复：

“Done”

或：

“已经完成”。
